[project]
name = "dataclaw-okf"
version = "0.1.0"
description = "Dataclaw plugin - Open Knowledge Format bundles for datasets"
requires-python = ">=3.12"
dependencies = [
    "dataclaw",
    "dataclaw-data",
]

[project.entry-points."dataclaw.plugins"]
dataclaw-okf = "dataclaw_okf:OKFPlugin"

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.24"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
asyncio_mode = "auto"
